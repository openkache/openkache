# OpenKache 로드맵

[English](./ROADMAP.md) · **한국어** · [中文](./ROADMAP.zh.md)

OpenKache는 SSD 우선(SSD-first) 캐시 서버입니다: SSD를 인메모리 인덱스 뒤의 스왑 대상으로
두는 대신, SSD를 주 용량 계층으로 삼고 RAM에는 압축 인덱스만 둡니다. 이 로드맵은 오늘 존재
하는 것, 지금 만들고 있는 것, 그리고 프로덕션 준비 릴리스로 가는 방향을 설명합니다.

상태 범례: ✅ 완료 · 🚧 진행 중 · 🔜 다음 · 📅 예정

## 현재 위치 (프리뷰)

현재 프리뷰는 하나의 서버 프로세스가 RESP/TCP로 Redis 호환 `GET`/`SET`/`DEL`을, QUIC/UDP로
네이티브 OpenKache Gate 0 프로토콜을 처리합니다. Linux에서는 `io_uring` 네트워크 프론트엔드와
다이렉트 I/O 스토리지 경로를 사용하며, 여러 키의 쓰기를 SSD에 대한 순차 세그먼트 그룹 쓰기로
묶습니다.

| 영역 | 상태 | 비고 |
|---|---|---|
| SSD 스토리지 엔진 | 🚧 | 세그먼트 그룹 쓰기 집계, Linux 다이렉트 I/O |
| RESP/TCP 프론트엔드 | 🚧 | Redis 호환 `GET`/`SET`/`DEL` |
| OpenKache/QUIC Gate 0 | 🚧 | QUIC 위 TLS 1.3; 로컬 개발용 신뢰 프로필 |
| 클라이언트 SDK | 🚧 | 하나의 생성 계약에서 나온 Rust, TypeScript, Python |
| 단일 바이너리 배포 | ✅ | 정적 musl 바이너리와 Linux amd64/arm64 컨테이너 |

## 다음: 프로덕션 하드닝 🔜

가장 가까운 마일스톤은 프리뷰를 운영자가 실제 데이터를 맡길 수 있는 것으로 바꾸는 것입니다.

- **재시작 복구.** 시작할 때마다 `openkache.data`를 초기화하는 대신, 재시작을 넘어 캐시를
  영속화하고 다시 로드합니다.
- **재현 가능한 벤치마크.** 누구나 돌려서 처리량·지연 수치를 재현할 수 있는 커밋된 하니스,
  그리고 GET/SET 혼합 워크로드.
- **운영 하드닝.** RESP·와이어 파서에 대한 퍼징, 모든 머지를 게이트하는 CI/CD, 특정 SSD에
  대한 용량 산정 가이드.

## 그다음: 보안과 정확성 📅

- **프로덕션 인증.** 로컬 개발용 Gate 0 프로필을 넘어, 실제 인증서 신뢰 체인을 갖춘 클라이언트
  인증과 상호 TLS.
- **값 보호 프로파일.** 전송 TLS 위에 얹는 클라이언트 측 값 보호
  (AES-256-GCM-SIV / AES-256-SIV-CMAC).
- **전체 프로토콜 표면.** 현재 Gate 0 오퍼레이션 하위 집합을 넘어 TTL 오버라이드, 조건부
  쓰기, 네임스페이스 관리, 통계.

## 이후: 확장과 저변 📅

- **클러스터링.** 일관된 해싱, 멤버십/가십, 복제, 페일오버를 구현해 배포가 단일 노드를 넘어설 수
  있도록.
- **크로스 플랫폼 서버.** 개발용 포터블 macOS 빌드와 Windows/WSL 경로를 마련해 기여자가 Linux에만
  묶이지 않도록. 최고 처리량은 여전히 Linux `io_uring` 이야기이며, 다른 플랫폼은 정확성과
  개발자 편의를 목표로 합니다.
- **정식 출시(GA).** 안정적인 API, 여러 언어로 게시된 패키지, 프로덕션 운영 문서.

## 조각들이 맞물리는 방식

생성된 프로토콜 계약 덕분에 새 오퍼레이션은 한 번 정의되면 모든 언어 클라이언트로 흘러갑니다.
그래서 프로토콜 작업과 클라이언트 작업이 따로 놀지 않고 함께 움직입니다. 네트워크·스토리지·
프로토콜 계층이 어떻게 분리되는지는 [docs/architecture.ko.md](./docs/architecture.ko.md)를,
이 로드맵의 한 조각을 맡는 방법은 [CONTRIBUTING.md](./CONTRIBUTING.md)를 참고하세요.
