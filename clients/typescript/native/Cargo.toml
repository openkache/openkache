[package]
name = "openkache-client-napi"
version = "0.1.0"
edition.workspace = true
description = "Node-API adapter for the OpenKache Rust client"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "*", default-features = false, features = ["async", "napi8", "serde-json"] }
napi-derive = "*"
openkache-client-core = { path = "../../core", default-features = false, features = ["quic-quinn", "tls-tcp"] }
serde_json = "*"

[build-dependencies]
napi-build = "*"
