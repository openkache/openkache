# OpenKache 路线图

[English](./ROADMAP.md) · [한국어](./ROADMAP.ko.md) · **中文**

OpenKache 是一款 SSD 优先(SSD-first)的缓存服务器:它把 SSD 作为主要容量层,而不是把 SSD
当作内存索引背后的交换目标,RAM 中只保留一份紧凑索引。本路线图描述今天已有的部分、正在构建的
部分,以及走向生产可用版本的方向。

状态图例:✅ 完成 · 🚧 进行中 · 🔜 下一步 · 📅 计划中

## 当前所处阶段(预览版)

当前预览版由一个服务器进程运行,通过 RESP/TCP 提供 Redis 兼容的 `GET`/`SET`/`DEL`,并通过
QUIC/UDP 提供原生 OpenKache Gate 0 协议。在 Linux 上,它使用 `io_uring` 网络前端和直接 I/O
存储路径;来自多个键的写入被合并为对 SSD 的顺序段组写入。

| 领域 | 状态 | 备注 |
|---|---|---|
| SSD 存储引擎 | 🚧 | 段组写入聚合,Linux 直接 I/O |
| RESP/TCP 前端 | 🚧 | Redis 兼容 `GET`/`SET`/`DEL` |
| OpenKache/QUIC Gate 0 | 🚧 | QUIC 之上的 TLS 1.3;本地开发信任配置 |
| 客户端 SDK | 🚧 | 由同一份生成契约产出的 Rust、TypeScript、Python |
| 单一二进制部署 | ✅ | 静态 musl 二进制与 Linux amd64/arm64 容器 |

## 下一步:生产加固 🔜

最近的里程碑是把预览版变成运维者能托付真实数据的东西。

- **重启恢复。** 跨重启持久化并重新加载缓存,而不是每次启动都截断 `openkache.data`。
- **可复现基准测试。** 一套已提交的、任何人都能运行以复现吞吐量与延迟数字的测试框架,以及
  GET/SET 混合工作负载。
- **运营加固。** 对 RESP 与线协议解析器的模糊测试、对每次合并把关的 CI/CD,以及针对特定 SSD
  的容量规划指南。

## 再之后:安全与正确性 📅

- **生产认证。** 超越本地开发 Gate 0 配置,提供带真实证书信任链的客户端认证与双向 TLS。
- **值保护配置。** 在传输层 TLS 之上叠加的客户端侧值保护
  (AES-256-GCM-SIV / AES-256-SIV-CMAC)。
- **完整协议面。** 超越当前 Gate 0 操作子集的 TTL 覆盖、条件写入、命名空间管理与统计。

## 更远:扩展与覆盖 📅

- **集群。** 一致性哈希、成员/gossip、复制与故障转移,让部署能够超越单个节点。
- **跨平台服务器。** 用于开发的可移植 macOS 构建以及 Windows/WSL 路径,让贡献者不局限于
  Linux。峰值吞吐量仍然是 Linux `io_uring` 的故事;其他平台以正确性和开发者体验为目标。
- **正式发布(GA)。** 稳定的 API、跨语言发布的包,以及生产运营文档。

## 各部分如何契合

生成的协议契约意味着一个新操作只需定义一次,就会流向每个语言客户端,因此协议工作与客户端工作
一起推进而不会各自漂移。关于网络、存储与协议层如何分离,见
[docs/architecture.zh.md](./docs/architecture.zh.md);关于如何接手本路线图中的某一块,见
[CONTRIBUTING.md](./CONTRIBUTING.md)。
