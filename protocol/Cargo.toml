[package]
name = "openkache-protocol"
version = "0.1.0"
edition.workspace = true
description = "Wire protocol types and codecs for OpenKache"
license = "Apache-2.0"

[dependencies]
crossbeam-queue = "*"
smallvec = { version = "*", features = ["const_new"] }
thiserror = "*"
vu128 = "*"
